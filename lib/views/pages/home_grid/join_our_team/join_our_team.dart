import 'package:idly_factory/core/export.dart';

class JoinOurTeam extends StatelessWidget {
  const JoinOurTeam({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("JoinOurTeam"),
      ),
    );
  }
}
