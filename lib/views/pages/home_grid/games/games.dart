import 'package:idly_factory/core/export.dart';

class TimeWordChallenge extends StatelessWidget {
  const TimeWordChallenge({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Games"),),);
  }
}
